import { Component } from '@angular/core';

@Component({
  selector: 'app-logo',
  standalone: true,
  templateUrl: './app-logo.component.html',
  styleUrl: './app-logo.component.scss',
})
/** Renders the application logo mark used in the sidebar header. */
export class AppLogoComponent {}
