spring:
  mail:
    default-encoding: UTF-8
    host: localhost
    username: ""
    password: ""
    # GreenMail's default SMTP test port
    port: 3025
    default-sender: user@example.com
    default-template: default
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false
    protocol: smtp
    test-connection: false
server:
  port: 50003
